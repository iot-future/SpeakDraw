/**
 * 预览服务器内部状态。
 * 当前无公开配置项，保留文件以备后续扩展。
 *
 * @internal
 */
// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface _PreviewServerInternal {
  // reserved for future configuration
}
